name: YouTube Digest Daily

on:
  schedule:
    # 北京时间每天 09:30 (UTC 01:30)
    - cron: '30 1 * * *'
  workflow_dispatch: # 手动触发

concurrency:
  group: youtube-digest
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          curl -fsSL https://deno.land/install.sh | sh
          echo "$HOME/.deno/bin" >> $GITHUB_PATH

      - name: Restore data from data branch
        continue-on-error: true
        run: |
          git fetch origin data 2>/dev/null || true
          for file in channels.json profile.json history.json feedback.json ranking_hints.txt; do
            git checkout origin/data -- "$file" 2>/dev/null || true
          done
          [ -f channels.json ] || cp channels.example.json channels.json
          [ -f profile.json ] || cp profile.example.json profile.json
          [ -f history.json ] || echo '{}' > history.json
          [ -f feedback.json ] || echo '{}' > feedback.json
          [ -f ranking_hints.txt ] || echo '' > ranking_hints.txt

      - name: Restore YouTube cookies
        run: echo "${{ secrets.YT_COOKIES_BASE64 }}" | base64 -d > cookies.txt

      - name: Analyze feedback and update preferences
        run: python update_preferences.py

      - name: Run digest
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_USER_ID: ${{ secrets.FEISHU_USER_ID }}
          FEISHU_CHAT_ID: ${{ secrets.FEISHU_CHAT_ID }}
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          MINIMAX_API_BASE: ${{ secrets.MINIMAX_API_BASE }}
          MINIMAX_MODEL: ${{ secrets.MINIMAX_MODEL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          YT_COOKIES_FILE: cookies.txt
          AIHOT_ENABLED: ${{ vars.AIHOT_ENABLED }}
          AIHOT_TAKE: ${{ vars.AIHOT_TAKE }}
          AIHOT_CANDIDATE_TAKE: ${{ vars.AIHOT_CANDIDATE_TAKE }}
          AIHOT_MIN_SCORE: ${{ vars.AIHOT_MIN_SCORE }}
          AIHOT_API_BASE: ${{ vars.AIHOT_API_BASE }}
        run: python main.py

      - name: Save data to data branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -B data
          git add -f channels.json profile.json history.json feedback.json ranking_hints.txt
          git commit -m "update data $(date -u +%Y-%m-%d)" || true
          git push origin data --force
